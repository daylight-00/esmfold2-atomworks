# @package _global_
# The searchpath lets an installed package find its own configs. It names only
# this package: Hydra warns on every compose for a searchpath entry it cannot
# resolve, so nothing else is listed.
hydra:
  searchpath:
    - pkg://esmfold2_atomworks.configs

defaults:
  - inference_engine: esmfold2
  - _self_
